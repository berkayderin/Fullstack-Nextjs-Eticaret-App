/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['firebasestorage.googleapis.com', 'productimages.hepsiburada.net', 'lh3.googleusercontent.com']
	}
}

export default nextConfig
